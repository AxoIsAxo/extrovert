<%- include('partials/header') %>
  <div class="card" style="display:flex;justify-content:space-between;align-items:center">
    <h2 style="margin:0">Chats</h2>
  </div>

  <% if (conversations.length === 0) { %>
    <div class="card center muted">
      <p>No conversations yet. Follow someone who follows you back to start chatting.</p>
    </div>
  <% } else { %>
    <% conversations.forEach(function(c){ %>
      <a href="/chats/<%= c.username %>" class="card conv-row">
        <% if (c.avatar) { %><img src="<%= c.avatar %>" alt="" class="avatar-img"><% } else { %><div class="avatar"><%= (c.display_name || '?').slice(0,1).toUpperCase() %></div><% } %>
        <div class="info">
          <b><%= c.display_name %></b>
          <small>@<%= c.username %></small>
          <div class="muted" style="font-size:0.85rem;overflow:hidden;text-overflow:ellipsis;white-space:nowrap"><%= c.last_message || '' %></div>
        </div>
        <% if (c.unread > 0) { %><sup class="badge"><%= c.unread %></sup><% } %>
      </a>
    <% }) %>
  <% } %>
<%- include('partials/footer') %>
